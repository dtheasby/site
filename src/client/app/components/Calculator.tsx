/*import * as React from 'react';

const Calculator = ({valueOne, valueTwo, result, handleChange }) => (
<div>
            <div>
                <input type="number" name="valueOne" value={valueOne} onChange={(e) => this.handleChange(e)} />
            </div>
            <div>
                <input type="number" name="valueTwo" value={valueTwo} onChange={(e) => this.handleChange(e)} />
            </div>
            <div>
                <select value={option} onChange={(e) => this.handleOption(e)}>
                    <option value="Add">Add</option>
                    <option value="Minus">Minus</option>
                </select>
            </div>
            <div>
                <p>result: {result}</p>
            </div>
        </div>
) 

export default Calculator;*/